#include <stdio.h>
int main()
{
    int T;
    scanf("%d", &T);
    for (int i = 1; i <= T; i++)
    {
        /* code */
        int N;
        scanf("%d", &N);
        for (int i = 0; i < N; i++)
        {
            /* code */
            if (N % i == 0)
            {
                /* code */
                printf("%d", N);
            }
            else
            {
                printf("%d", N);
            }
        }
    }
}